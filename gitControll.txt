cd d:/projects/AdminHK
1.将本地的仓库和github上同步(拉)
git pull origin master

2.修改代码

3.查看仓库状态
git status

4.新增、修改文件后
//添加所有文件
git add "*" 

5.查看仓库状态
git status

6.将修改后的文件加入仓库
git commit -m "XXX"

7.将本地仓库上传到github(推)
git push origin master
